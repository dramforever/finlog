proc main {
    var a: u32 = 1u32;
    var b: u32 = 2u32;
    var c: u32 = 3u32;
    loop {
        if (a + c) {
            a = a + 1u32;
            yield;
        }

        if (a + c) {
            a = a + 1u32;
        }

        yield;
    }
}
